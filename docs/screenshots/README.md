# Screenshots

Drop the images here with these exact names, then replace the screenshot
comment in the root [`README.md`](../../README.md) with the real table.

| File | Capture | Notes |
| --- | --- | --- |
| `landing.png` | The marketing page at `/` | Its own atmospheric visual system, distinct from the app. |
| `today.png` | **Today** | The safe-to-spend hero circle + stat grid + a couple of logged rows. |
| `trends.png` | **Trends** | The stacked bar chart with the average line + the "patterns we spotted" cards. |
| `wrapped.png` | **Reflect → ▶ play your month** | One Wrapped story card — the total, the leaderboard, or the final receipt. |
| `dark-mode.png` | Any screen, dark theme | Settings → appearance → Dark, then Today or Envelopes. |
| `mobile.png` | Mobile layout | Narrow the window so the bottom nav + FAB show; Today or Receipts. |

Suggested widths: ~1400px for desktop shots, ~450px for the mobile shot.
PNG, no device frame — the neubrutalist UI is the frame.
