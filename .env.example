# Required for the AI-written month summary on Today (api/insights.js).
# Get a key at https://console.anthropic.com — set this in Vercel's project
# env vars for prod, and in a local .env.local (gitignored) for `npm run dev`.
ANTHROPIC_API_KEY=
